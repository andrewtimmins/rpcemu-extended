Per-machine data lives here: one directory per machine, holding its CMOS,
its HostFS disc and any hard disc images.

RPCEmu creates them for you: use New... in the machine selector. This file
exists only so the directory is present in a fresh checkout.
