# The emulator core is a large C project; give the daemon room and keep the
# configuration cache off, because the CMake external build is not compatible
# with it in this AGP version.
org.gradle.jvmargs=-Xmx3g
org.gradle.parallel=true
android.useAndroidX=true
android.nonTransitiveRClass=true
